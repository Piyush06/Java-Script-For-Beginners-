//Javascript can change HTML Styles (CSS)
//changing the style of an HTML element, is a variant of changing an HTML attribute:

document.getElementBYId("demo") .styles.fontSize = "25px";

//You can change the coefficient values as per your requirements or to test your Code.
